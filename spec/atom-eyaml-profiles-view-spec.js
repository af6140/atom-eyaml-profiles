'use babel';

import AtomEyamlProfilesView from '../lib/atom-eyaml-profiles-view';

describe('AtomEyamlProfilesView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
